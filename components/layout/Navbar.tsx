import { AppBar, Box, Toolbar } from '@mui/material'

const Navbar = () => {
  return (
    <AppBar color="primary">
      <Toolbar>
        <Box flex={0.1}>hello</Box>
      </Toolbar>
    </AppBar>
  )
}

export default Navbar
